import React from 'react';
import { getAuth } from 'firebase/auth';

export default function LoginUser({user}) {
    console.log(user);
    return (
        <div>
            <h6></h6>
        </div>
    );
}

